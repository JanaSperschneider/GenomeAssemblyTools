#################################################################################################################################################################
library(ggplot2)
#---------------------------------
#---------------------------------
#---------------------------------
df <- read.delim('example_bincoverage.txt', sep="\t", header=TRUE, skip=2)
head(df)
#---------------------------------
#---------------------------------
data <- subset(df, select = c("Cov"))

range_to_show = 200

ggplot(data, aes(x=Cov)) + geom_histogram(binwidth=1) + 
  xlim(0, range_to_show)
